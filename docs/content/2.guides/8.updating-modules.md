---
title: 更新 Nuxt 模块
description: 保持 Nuxt SEO 模块更新，以获得缺陷修复和新功能。
relatedPages:
  - path: /docs/nuxt-seo/getting-started/troubleshooting
    title: 故障排查
  - path: /docs/nuxt-seo/guides/debugging-modules
    title: 调试模块
---

## 更新命令

由于 `@nuxtjs/seo` 捆绑了核心子模块，你只需要更新主包。像 `nuxt-skew-protection` 和 `nuxt-ai-ready` 这样的独立模块需要单独更新。

::code-group

```bash [pnpm]
pnpm update @nuxtjs/seo --latest
```

```bash [npm]
npm update @nuxtjs/seo@latest
```

```bash [yarn]
yarn upgrade @nuxtjs/seo --latest
```

```bash [bun]
bun update @nuxtjs/seo --latest
```

::

## 更新卡住时

包管理器可能会缓存旧版本。如果更新没有生效：

::code-group

```bash [pnpm]
rm -rf node_modules pnpm-lock.yaml && pnpm install
```

```bash [npm]
rm -rf node_modules package-lock.json && npm install
```

```bash [yarn]
rm -rf node_modules yarn.lock && yarn install
```

```bash [bun]
rm -rf node_modules bun.lockb && bun install
```

::

## 检查破坏性变更

更新主版本前，先查看 [GitHub Releases](https://github.com/nuxt-modules/seo/releases) 中的迁移说明。

各模块的 changelog：

- [Sitemap](https://github.com/nuxt-modules/sitemap/releases)
- [Robots](https://github.com/nuxt-modules/robots/releases)
- [OG Image](https://github.com/nuxt-modules/og-image/releases)
- [Schema.org](https://github.com/nuxt-modules/schema-org/releases)
- [Link Checker](https://github.com/nuxt-modules/link-checker/releases)
- [SEO Utils](https://github.com/nuxt-modules/seo-utils/releases)

## 验证更新

更新后，检查已安装的版本：

```bash
npx nuxt info
```

在模块列表中查找 `@nuxtjs/seo`。
